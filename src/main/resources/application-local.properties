# Port used to deploy the application locally
server.port = 8082

# Mongodb properties
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=task-list

# Frontend application path
frontend.application.path=http://localhost:3000

# Ms paths
ms.auth.url=http://localhost:8083

# Authorization endpoint
auth.url.validate=/api/validate
